# 📈 Website Traffic Forecasting (C++ / Holt-Winters)

Приложение на C++ для **прогнозирования трафика веб-сайта** на основе временных рядов.  
Использует **тройное экспоненциальное сглаживание (Holt-Winters, аддитивная модель)** для предсказания количества посетителей на следующие дни.

---

## 🗂️ Датасет

**Источник:** [Daily Website Visitors — Kaggle](https://www.kaggle.com/datasets/bobnau/daily-website-visitors)  
**Описание:** ежедневные данные о количестве посетителей сайта.  
Формат CSV:  

| Row | Day      | Day of week | Date      | Page Loads | Unique Visits | First Time Visits | Returning Visits | 
|-----|----------|-------------|-----------|------------|---------------|-------------------|------------------|
| 1   | Friday   | 6           | 10/3/2014 | "3,005"    | "2,097"       | "1,856"           | 241              |
| 2   | Saturday | 7           | 10/4/2014 | "2,054"    | "1,436"       | "1,274"           | 162              |
| ... | ...      | ...         | ...       | ...        | ...           | ...               | ...              |

---

## ⚙️ Возможности

- Чтение данных из CSV (`date, visitors`)
- Автоматический подбор параметров α, β, γ по WAPE-валидации  
- Поддержка сезонности (по умолчанию `m = 7`, недельный цикл)
- Прогнозирование для всех метрик:
  - Page Loads
  - Unique Visitors
  - First Time Visitors
  - Returning Visitors
- Сохранение прогноза в CSV файл
- Настройка горизонта прогноза (`H`)
- **Шифрование файлов** с использованием алгоритма SEED (128-битный ключ)
- **Расшифровка файлов**
- Генерация случайных ключей шифрования

---

## 🔐 Криптография

Приложение поддерживает шифрование и расшифровку файлов с использованием алгоритма **SEED**:
- 128-битный симметричный блочный шифр
- Режим CBC (Cipher Block Chaining)
- Паддинг PKCS7

---

## 🧰 Установка

### Arch Linux / Manjaro

```bash
sudo pacman -S --needed base-devel cmake
git clone https://github.com/yarikpd/website-traffic-forecast-cpp.git
cd website-traffic-forecast-cpp
mkdir build && cd build
cmake ..
cmake --build .
```

### Ubuntu / Debian

```bash
sudo apt update
sudo apt install build-essential cmake git
git clone https://github.com/yarikpd/website-traffic-forecast-cpp.git
cd website-traffic-forecast-cpp
mkdir build && cd build
cmake ..
cmake --build .
```

---

## 🚀 Использование

```bash
./traffic_forecast <csv_path> [опции]
```

**Параметры:**

| Параметр | Описание |
|----------|----------|
| `<csv_path>` | Путь к входному CSV файлу с данными (обязательный) |
| `--output <path>` | Путь к выходному файлу для прогноза (по умолчанию `forecast.csv`) |
| `--H <n>` | Горизонт прогноза в днях (по умолчанию 30) |
| `--season_m <n>` | Длина сезона (по умолчанию 7) |
| `--crypt <key_file>` | Путь к файлу с ключом шифрования |
| `--newCryptKey <key_file>` | Генерация нового ключа и сохранение в файл |
| `--encrypt <output_file>` | Шифрование файла `csv_path` и сохранение в `output_file` |
| `--decrypt <output_file>` | Расшифровка файла `csv_path` и сохранение в `output_file` |
| `--help`, `-h` | Вывод справки |

### Примеры

**Прогнозирование:**

```bash
./traffic_forecast ../dataset.csv --H 30 --season_m 7 --output forecast.csv
```

**Генерация ключа и шифрование файла:**

```bash
./traffic_forecast data.csv --newCryptKey key.bin --encrypt encrypted.bin
```

**Расшифровка файла:**

```bash
./traffic_forecast encrypted.bin --crypt key.bin --decrypt decrypted.csv
```

---

## 📊 Вывод программы

Пример консольного отчёта:

```
Загрузка датасета из CSV...
Датасет загружен, строк: 2167
Прогноз сохранён в forecast.csv
----------
Сезоны m: 7
Количество прогнозируемых точек H: 30
Page Loads коэффициенты: alpha=0.3, beta=0.1, gamma=0.2, WAPETest=0.0845
Unique Visitors коэффициенты: alpha=0.4, beta=0.1, gamma=0.3, WAPETest=0.0912
First Time Visitors коэффициенты: alpha=0.3, beta=0.1, gamma=0.2, WAPETest=0.0978
Returning Visitors коэффициенты: alpha=0.5, beta=0.2, gamma=0.4, WAPETest=0.1234
```

---

## 📁 Структура проекта

```
traffic-forecast/
├── CMakeLists.txt          # Конфигурация сборки
├── main.cpp                # Точка входа
├── dataset.csv             # Пример датасета
├── README.md               # Документация
├── crypt/                  # Модуль шифрования SEED
│   ├── crypt.h
│   └── crypt.cpp
├── dataset/                # Модуль работы с данными
│   ├── dataset/
│   │   ├── Dataset.h
│   │   └── Dataset.cpp
│   └── dataset_value/
│       ├── DatasetValue.h
│       └── DatasetValue.cpp
├── forecast/               # Модуль прогнозирования
│   ├── forecast.h
│   └── forecast.cpp
├── forecast_utils/         # Вспомогательные функции
│   ├── forecast_utils.h
│   └── forecast_utils.cpp
└── tests/                  # Тесты (Google Test)
    ├── test_crypt.cpp
    ├── test_dataset.cpp
    └── test_dataset_value.cpp
```

---

## 🧪 Тестирование

Проект использует **Google Test** для модульного тестирования.

```bash
cd build
ctest --output-on-failure
```

Или запустить тесты напрямую:

```bash
./dataset_test
./dataset_value_test
./crypt_test
```

---

## 🧾 Лицензия

MIT License  
© 2025